<%
  rendered_content = escape_javascript(render :partial => 'tips')
  rendered_content += escape_javascript(render 'index', :banners => @banners, :countings => @countings)
%>

$('#ajax_content').html("<%= rendered_content %>");

prepare_paginated_links('top');
init_slider();

$('#loader').hide();